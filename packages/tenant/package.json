{
  "name": "@dvelop-sdk/tenant",
  "version": "0.1.0",
  "description": "Package tenant contains functions to determine for which tenant a request should be served.",
  "author": "Martin Testrot",
  "homepage": "https://github.com/d-velop/dvelop-sdk-node#readme",
  "license": "Apache-2.0",
  "main": "./lib/index.js",
  "directories": {
    "lib": "lib",
    "test": "__tests__"
  },
  "files": [
    "lib"
  ],
  "repository": {
    "type": "git",
    "url": "git+https://github.com/d-velop/dvelop-sdk-node.git"
  },
  "bugs": {
    "url": "https://github.com/d-velop/dvelop-sdk-node/issues"
  },
  "scripts": {
    "test": "mocha \"./**/__tests__/**/*.ts\"",
    "watch-test": "mocha \"./**/__tests__/**/*.ts\" --watch",
    "build": "tsc -b",
    "watch": "tsc -b --watch",
    "clean": "tsc -b --clean",
    "cleanbuild": "npm run clean && tsc -b --force",
    "prepublish": "npm run cleanbuild"
  },
  "devDependencies": {
    "mocha": "^8.2.1",
    "typescript": "^4.1.2"
  }
}
