{
  "name": "dvelop-sdk",
  "private": true,
  "description": "Official SDK to build d.velop cloud Apps with Node.js",
  "license": "Apache-2.0",
  "scripts": {
    "test": "mocha \"./packages/**/__tests__/**/*.ts\"",
    "watch-test": "mocha \"./packages/**/__tests__/**/*.ts\" --watch",
    "build": "tsc -b",
    "watch": "tsc -b --watch",
    "clean": "tsc -b --clean",
    "cleanbuild": "npm run clean && tsc -b --force",
    "prepublish": "npm run cleanbuild"
  },
  "devDependencies": {
    "@types/chai": "^4.2.14",
    "@types/mocha": "^8.0.4",
    "@types/node": "^14.14.10",
    "chai": "^4.2.0",
    "lerna": "^3.22.1",
    "mocha": "^8.2.1",
    "ts-node": "^9.0.0",
    "typescript": "^4.1.2"
  }
}
